[phases.setup]
nixPkgs = ["php84", "php84Packages.composer", "nginx", "libmysqlclient"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.build]
cmds = []

[start]
cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=8080"
